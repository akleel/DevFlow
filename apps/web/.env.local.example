# Frontend (Next.js)

API_URL=http://localhost:3001
ADMIN_TOKEN=change-me

# Server-only toggle to enable the dev-only admin proxy routes.
ENABLE_ADMIN=false

ADMIN_GATE=change-me

# Client-side toggle to show/hide the /admin UI.
NEXT_PUBLIC_ENABLE_ADMIN=false